/**
* I don't want to test the scheduled tasks since they trigger execution of other parts of
* the system in potentially unexpected ways. But, I'm leaving this service here with this
* comment to make sure that we don't try to re-create the test harness later on (unless we
* come back to it with a more tempered perspective).
*/
component extends="spec.BaseTest" {

	// ... nothing to do for now.

}
